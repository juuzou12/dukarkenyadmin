class Products{
  final String productName, quantity,about,productId,sellerID;
  final String price;

  Products(this.productName, this.price, this.quantity, this.about, this.productId, this.sellerID);
}